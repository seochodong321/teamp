{
  "name": "teamp",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/gen-sw.mjs && vite",
    "build": "node scripts/gen-sw.mjs && vite build",
    "build:ci": "node scripts/gen-sw.mjs --production && vite build",
    "preview": "vite preview",
    "gen-sw": "node scripts/gen-sw.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:rules": "firebase emulators:exec --only firestore \"vitest run --config vitest.rules.config.js\""
  },
  "dependencies": {
    "date-fns": "^3.3.1",
    "firebase": "^12.12.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.0",
    "zustand": "^4.5.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@vitejs/plugin-react": "^4.2.1",
    "playwright": "^1.60.0",
    "vite": "^5.1.0",
    "vitest": "^4.1.8"
  }
}
